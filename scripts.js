function changeLanguage() {
    const lang = document.getElementById('language').value;
    // Aquí puedes implementar el cambio de idioma, por ejemplo, cargando el contenido adecuado
    // o usando una biblioteca de internacionalización.
    alert("Idioma cambiado a: " + lang);
}
